apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  # secret.yaml 은 git 에 없다. secret.example.yaml 을 복사해 값을 채운다.
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
